using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlotManager : MonoBehaviour
{
    // √еймћенеджер Ѕудет брать ссылку с объекта на сцене на котором висит некий сюжетный менеджер и внем уже будет прописанно сама анимаци€ дл€ сцены
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
